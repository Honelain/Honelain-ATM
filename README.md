# Honelain-ATM
My first Python project after a month of learning to practice loops, conditionals, inputsand file operations. 

I'm always open to learning so as to improve and become better.

#I built this to practice:
* Implementing infinite systems loops ('while True')
* Conditional logic ('if', 'elif', 'else')
* Input handling and data validation ('try/except')
* Simple file reading and writing ('pins.txt', 'read()', 'write()')
